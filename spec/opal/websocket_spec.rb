require "spec_helper"

RSpec.describe Opal::Websocket do
  it "has a version number" do
    expect(Opal::Websocket::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
